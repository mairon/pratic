<h1 align="right" id="header">Cidades</h1>
<%= link_to 'Volver', cidades_path %>
<% form_for(@cidade) do |f| %>
  <%= f.error_messages %>

<div class="group">
  <h2>Cidade</h2>
  <table>
    <tr>
      <td><b>Cidade :</b></td>
      <td><%= f.text_field :nome, 
                           :size => '30',
                           :onkeydown => "f(this)",
                           :onkeyup => "f(this)"%></td>
    </tr>

    <tr>
      <td><b>Estado :</b></td>
      <td><%= f.collection_select :estado_id, Estado.all(:order => '1'),:id,:nome %></td>
    </tr>
  </table>
</div>
    <%= f.submit "Grabar" %> |
    <%= link_to 'Volver', cidades_path %>
<% end %>


